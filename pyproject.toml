[project]
name = "gpt-league"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "gpt-racing",
]

[tool.uv.sources]
gpt-racing = { git = "https://github.com/logangrado/gpt-racing.git", tag = "v0.6.1" }
